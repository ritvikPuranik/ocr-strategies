# OCR Experiments Requirements

# Approach 1: OCR-only (Tesseract)
pytesseract>=0.3.10
Pillow>=10.0.0

# Approach 2: OCR + Layout (PaddleOCR 3.x with structure detection)
paddlepaddle>=2.5.0
paddleocr[doc-parser]>=3.0.0

# Approach 3: OCR-free VLM (Donut)
transformers>=4.35.0
torch>=2.0.0
sentencepiece>=0.1.99

# Approach 4: OCR + LLM Grounding
openai>=1.0.0
# anthropic>=0.18.0  # uncomment if using Claude

# Utilities
tqdm>=4.65.0
